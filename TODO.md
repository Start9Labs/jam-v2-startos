# TODO

- [ ] Move to a stable Jam 2.x image once upstream publishes one (see `UPDATING.md`).
- [ ] Runtime-verify on a StartOS host: wallet creation, chain rescan, and that the orderbook watcher reaches the directory nodes (`curl http://127.0.0.1:8000/health` → `directory_nodes > 0`).
